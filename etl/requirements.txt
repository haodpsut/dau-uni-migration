# Postgres driver (psycopg v3 with C bindings)
psycopg[binary]>=3.2.0

# SQL Server driver (pure Python, no ODBC needed)
pymssql>=2.3.0

# Config
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Logging
loguru>=0.7.0

# CLI + progress
typer>=0.12.0
rich>=13.7.0

# MinIO (S3-compatible)
minio>=7.2.0

# Google Drive download (public folder/file — no OAuth)
gdown>=5.2.0

# Data manipulation
pandas>=2.2.0           # optional, dùng cho transform phức tạp
